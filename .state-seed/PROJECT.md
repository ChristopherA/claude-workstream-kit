# Project Context

Read by `/workstream-status` to frame the project's status. It is a list
rather than a scan because a scan of `.claude/skills/` cannot tell a skill
unique to this project from one copied in from a shared source, and only
what is unique to this project frames its status. Nothing outside this
list and `.state/` is read for that purpose.

## Unique skills

<!-- one per line, the directory name under .claude/skills/:  - skill-name -->

## Context files

<!-- one per line, a path from the project root:  - docs/overview.md -->
